export default function SpendView({ data }) {
  return (
    <section className="view-stub">
      <h2 className="view-title">Spend</h2>
      <p className="muted">Spend rollup and recharts bar chart (pending vs approved by tier/property). Full chart coming in Task 10.</p>
    </section>
  );
}

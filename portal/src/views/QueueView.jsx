export default function QueueView({ data, reload }) {
  return (
    <section className="view-stub">
      <h2 className="view-title">Queue</h2>
      <p className="muted">Items awaiting decision (Stage = Submitted). Full list + detail pane coming in Task 5.</p>
    </section>
  );
}

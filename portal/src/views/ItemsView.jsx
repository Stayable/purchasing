export default function ItemsView({ data, reload }) {
  return (
    <section className="view-stub">
      <h2 className="view-title">Items</h2>
      <p className="muted">All procurement items, sortable by stage and spend. Full list + detail pane coming in Task 5.</p>
    </section>
  );
}

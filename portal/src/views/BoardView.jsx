export default function BoardView({ data, reload }) {
  return (
    <section className="view-stub">
      <h2 className="view-title">Board</h2>
      <p className="muted">Items grouped by Stage (kanban-style). Full board view coming in Task 5.</p>
    </section>
  );
}

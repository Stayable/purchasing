export default function DecisionsView({ data, reload }) {
  return (
    <section className="view-stub">
      <h2 className="view-title">Decisions</h2>
      <p className="muted">Log of decided items (Approved / Approved-with-Conditions / Declined) with notes. After-the-fact note edit coming in Task 9.</p>
    </section>
  );
}
